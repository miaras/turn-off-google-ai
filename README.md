This extension turns off the awful Google AI overview by appending "-ai" to every search result.

It's a simple solution, but it works.

Usage:

`git clone https://github.com/miaras/turn-off-google-ai.git`

Go to `chrome://extensions`

Click "Load unpacked"

Click cloned directory
